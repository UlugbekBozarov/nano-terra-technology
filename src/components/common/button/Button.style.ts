import { Button } from "antd";
import { styled } from "styled-components";

export const StyledButton = styled(Button)({
  height: "40px",
  display: "flex",
  alignItems: "center",
  padding: "10px 24px",
});
