import { Table } from "antd";
import { styled } from "styled-components";

export const StyledTable = styled(Table)({
  "& .ant-table-header": {
    borderRadius: "8px",
    border: "1px solid",
  },
});
